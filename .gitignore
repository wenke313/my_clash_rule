# AI 辅助文件（本地用，不入公开仓库）
# AGENTS.md 含路由器 secret/IP 等敏感信息；CLAUDE.md 是 Claude Code 本地指引
AGENTS.md
CLAUDE.md

# 系统文件
.DS_Store
Thumbs.db

# 节点测试环境（含订阅密钥/exe/结果，不入库）
bench/

# 本地工作记录（分析/排查过程，不入库）
logs/

# 本地参考文档（含路由器体系速查，不入库，仅本机 Agent 共享）
docs/

# 本机 Agent 技能/配置（不入公开仓库）
.pi/

# 已废弃项目（本地保留，不入库）
guard/
smart_feed/

# 敏感主配置（含订阅 token，绝不入库；Clash-v3-smart.yaml 等放此目录，github 同步自动排除）
config/
BT_Tracker.yaml

# mihomo geo 数据文件（二进制，测试用）
GeoIP.dat
GeoSite.dat

# 临时文件
.tmp/
